name: CI-CD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Build Backend
      run: docker build -t apma-backend ./backend

    - name: Build Frontend
      run: docker build -t apma-frontend ./frontend
